# Architecture

Authoring guidance: the architecture standard.

## Overview

A retrieval system over one corpus, the consolidated EU AI Act, where the corpus carries its own link structure and the answers have to be traceable.

Five stages in request order. A plain-language system description arrives at a web frontend and reaches a Python service. An agent routes the question, retrieves candidate passages by meaning, walks the Act's cross-references outward from those passages, synthesizes an answer citing what it read, and verifies each claim against the retrieved text before returning it. A claim that cannot be grounded is dropped, and a question left unsettled by the text returns a refusal instead of an answer.

The graph and the vector index are two views of the same corpus rather than two systems. Semantic search finds where to start. Traversal finds what that start point depends on.

## Key technical decisions

### The reference graph is parsed, not extracted

The Act ships as structured legal text with numbered articles, annexes and recitals, and its cross-references are written into the sentences themselves. That makes the graph a parsing job over an explicit reference structure rather than an extraction job handing text to a model and trusting what comes back.

Parsing wins twice. It is roughly a day cheaper than model extraction, and it is checkable: a parsed edge either matches a reference in the text or it does not, where an extracted edge is only ever as good as the extraction. The Official Journal text carries 113 articles, 13 annexes and 180 recitals, and the text consolidated at 2026-07-27 carries 119 articles, 14 annexes and no recitals. The shipped predicate yields 523 reference edges on the first and 607 on the second. An earlier figure of 518 phrases appears in the record with no rule attached and no rule reproduces it, so it is retired rather than carried. Measured in PR #2 on 2026-09-06. On a corpus whose whole value is traceability, a graph nobody can verify would undercut the thing being built.

### The whole Act is ingested, and only the demo question is narrowed

An early proposal cut the corpus down to Article 50, on the grounds that the high-risk obligations were postponed. That confuses two things. The postponement changes when obligations bite. It removes no cross-reference from the text.

The reference structure is what the graph is built from, and the richest chain in the Act runs from Article 6 to the obligation articles and on to conformity assessment. Traversal reaches all eight obligation articles and Article 43 from Article 6 in two hops, in both versions. It does not pass through Annex III, which cites only Article 6 and Article 6(2), so the chain is a reachability claim rather than the four-hop route the earlier wording drew. Measured in PR #2 on 2026-09-06. Cutting the corpus to one article would remove the only part of the text where traversal visibly beats semantic search, and parsing every article costs nothing over parsing one.

### Refusal is a feature, not a safety net

The system reports which articles to read. It does not say whether an organization complies. Where the text genuinely leaves a question open, it says so and names what is missing.

This is a product decision before it is a safety one. A tool that always answers is indistinguishable from a tool that guesses, and on a legal corpus the cost of a confident wrong answer is the whole downside. Grandfathering is the worked example: systems already on the market before the amended deadlines fall outside full high-risk compliance unless later substantially modified, and no regulator has defined that threshold. The text does not settle it, so neither does the system.

The decision stands and the gap it names has half closed. The evaluation puts three questions the text does not settle to each arm on each version, which is eighteen chances to refuse correctly, and the arms took five at v0.6 and four at v0.9. Refusing wrongly is where the movement is: false refusals fell from eight to three, and the pattern that made the v0.6 eight worth writing about, every retrieval arm refusing the version-comparison flow, is gone. That flow was not a refusal defect. `ROUTE` asked for the obligations a description touches and got exactly that, so a question about when an obligation applies had its timing discarded, and Article 113, the only article in either version carrying the compliance dates, was never searched for. Replaying the refusing runs put every one of them at the declared `REFUSE` marker, and one named Article 113(3), point (a) as the text it had not been handed. The prompt asks for the timing now as well. Refusing was never the sharper half: three recorded runs answered a deadline question out of provisions carrying no date and scored as successes, so a fourth refusal exit drops a timing answer whose statements give no date their own citations carry, which caught the one replayed run that had crossed from refusing into answering. Retrieval was the half that had not followed the prompt, and the embedder swap supplied it. All three deadline questions now score 1.00 on both documents in the traversal arm, each reaching Article 113 from Article 111, so the flow that concentrated every false refusal is answered rather than declined. Search alone still misses Article 113 on the original text, where two of its three chunks were Official Journal footnotes the parse swallowed, and the walk is what closes that. What remains is the correct-refusal half, which moved the wrong way, from five in eighteen to four: at that size the count is a direction rather than a rate, and it is the weakest measured behavior in a system whose headline property it is. Measured in PR #7 on 2026-09-07.

`feature-corpus-and-answer-text` fixed the parse this paragraph names: `art_113` no longer carries the footnote apparatus, so it no longer splits into three chunks with two of them off subject. Whether that moves search's own recall of Article 113 on the original text is unmeasured here, since re-running the harness is `v3.2`'s to do. Fixed at this branch on 2026-09-13.

### Both versions of the text are indexed

The Digital Omnibus, Regulation (EU) 2026/1744, amended the Act on 27 July 2026 and moved two of the three compliance deadlines. A system reading only the original text answers deadline questions confidently and wrongly.

Indexing the original alongside the amended text turns a staleness bug into an observable property: an answer that changed between versions can say so and show which citation moved. That covers data freshness with a real amendment rather than a hypothetical one.

### The evaluation is the deliverable, not a feature of it

The harness answers the same question set three ways: full-context stuffing, vector retrieval alone, and vector retrieval plus reference traversal. It reports accuracy and cost per arm.

This inverts the usual order because the premise demands it. The Act fits a current context window, so a model can read the whole document and answer from it. Both versions were put through `qwen3.8:27b` and the token counts read back off it rather than estimated: the original is 581 082 characters over its articles, annexes and recitals and comes back as **114 720 prompt tokens**, and the consolidated is 384 515 characters and **77 040**. Both sit inside the 131 072 this machine can hold GPU-resident, so the baseline arm is not constrained on either side. Prompt evaluation alone took 83 seconds and 35 seconds. Measured in PR #2 on 2026-09-06. Prompt caching removes most of the per-query cost argument on a fixed corpus. Retrieval is therefore not obviously justified here, and a pipeline shipped without the comparison is one nobody can defend.

The figure of roughly 145 000 tokens this entry used to carry was 582 489 characters divided by four, an estimate nobody had checked. The real ratio is 5.07 on the original and 4.99 on the consolidated. Recording the correction rather than deleting the claim, because the same bad ratio also decided an early version of the chunking rule.

Faithfulness is the metric that carries the most weight. Retrieval can be correct while the synthesized answer drifts from what was retrieved, and on a legal corpus that drift is the failure mode with consequences. It is scored as span containment against what the model was supplied rather than against what its claims cite, since the second is already enforced by the grounding check and would report the threshold back rather than the arm. What that measure cannot do is compare arms without its denominator beside it, because an arm supplied with the whole document is matched against the whole document's vocabulary.

The harness is built and has been run twice, and the baseline won both times. At v0.6, over 72 runs: full context reached every gold provision, search alone reached 0.41 and 0.46 of them, and search plus traversal 0.54 and 0.56. Measured in PR #3 on 2026-09-06. At v0.9 the same harness on `snowflake-arctic-embed2` gave 0.61 and 0.71 for search alone and 0.74 and 0.81 with the walk, over 72 runs with none failing. Measured in PR #7 on 2026-09-07. Retrieval's case on this corpus is still cost and checkability rather than accuracy. `docs/evaluation.md` carries the numbers, `python/data/eval/results.json` carries the current run and `python/data/eval/runs/` the one before it, including the routed query and the provision ids each arm searched, traversed and dropped on every question. This entry keeps the decision and that document keeps the numbers, so a figure moves in one place.

One claim above needed qualifying at v0.6 and needs re-qualifying now, because the qualification has changed rather than gone. Traversal reaching every obligation article and Article 43 from Article 6 in two hops is a property of the graph, asserted by `python/tests/corpus/test_graph.py` and still true. At v0.6 it was not a property of the pipeline because search rarely returned Article 6 to walk from. The swap fixed that half: on `q06-worker-promotion` search now returns `anx_III` and `art_6.3`, the walk lifts the paragraph to Article 6, and it reaches Article 10 through Article 15 and Article 43.

The arm still scores 0.27 on that question, because `Pipeline._within_budget` then drops every one of them to fit the 32 768-token window. Scored against everything the walk reached, the arm makes 0.89 on the original text against the 0.74 it delivers. **The bottleneck moved from the embedder to the synthesis prompt budget**, and it is now a question about how much retrieved text a window can carry rather than one about finding the text. `RetrievalTrace.dropped_ids` is what makes that visible instead of silent, and no decision above changes on it.

The bottleneck did not move a second time, but its size did, and the 0.74 and 0.89 above are superseded rather than current. `_within_budget` filled that window in arrival order, search hits before traversal, so a recital search returned outranked an article the walk actually reached. On `q04`, `q05` and `q06` of the original text recitals are 8 to 10 of the 12 search hits and 0 of the 40 traversed provisions, so what filled the window first was never the walk's own doing. It now sorts every citation by `(kind is RECITAL, position)` before the greedy fill, so a recital spends only the budget nothing else wants. `q06` scores 0.73 rather than 0.27, keeping `art_10` through `art_15` that recitals used to crowd out, and the arm's mean recall on the original text moves from 0.74 to 0.83 against the same 0.89 ceiling the walk reaches. `art_43` still drops on `q04` and `q06`, and `art_8` and `art_9` never enter either question's traversed set at all, excluded by the 40-node cap before the budget runs. The consolidated text carries no recitals to reorder, so its 0.81 does not move. Measured at this branch on 2026-09-08, over 72 runs. `docs/evaluation.md` carries the per-question figures.

The graph the arms call gained a sixth node on `feat/live-walk`. The budget cut moved out of `synthesize` into a `budget` node between `traverse` and `synthesize`, because a node's result reaches a stream frame only when the node finishes, and the page has to name what the model is reading before the model call ends. The node carries the kept `Citation` tuple on the state verbatim, in its sorted order and with its repeats, since `parse_draft` and `_uncited_ids` number citations by position. A throwaway run at `003df46` over eight scripted cases, including a cut at a 1024 window, traversal off, a declared refusal and a retried cut, produced identical synthesis prompts and identical answers against the stock graph. `python/tests/agent/test_pipeline.py` now pins the budget's supplied ids to the prompt's own numbering, so the figures above do not move on this change and `v3.2`'s re-run measures the same cut. Measured at `003df46` on 2026-09-13.

Cut a retrieval arm before cutting the eval.

### Full-context stuffing is a first-class arm, not a straw man

The baseline gets the same corpus, the same questions, and prompt caching where the provider offers it. Weakening it would make the comparison worthless.

Two things survive if the baseline wins on accuracy, and both are worth stating rather than hiding. Retrieval means the exact passages sent are known, so a citation can be checked against them programmatically rather than trusted. And the technique matters one size up: this document fits a context window, while a national implementation plus guidance plus standards plus case law does not.

### Four production concerns are built, three are reasoned about

Cost and token control, failure modes, data freshness and CI are implemented. Prompt injection, latency and observability are designed for and documented, not built.

This entry used to read three and four, and v0.4 moved CI across by shipping the workflow. The split is recorded rather than corrected in place, because which concerns were cut and why is itself part of the record and a heading that quietly renumbers loses that.

The window does not fit all seven at a quality worth showing, and thin coverage of seven is worse than four that hold. `docs/evaluation.md` carries the per-concern account, being what each one would have cost and what standing in for it looks like.

## Risks / open questions

- **Vector store. Settled: `sqlite-vec`.** Exercised on this interpreter rather than assumed, which is the check `networkx` failed. `sqlite_vec` 0.1.9 loads on Python 3.14.1, `vec_version()` returns `v0.1.9`, and a `vec0` virtual table over `float[1024]` is created against SQLite 3.50.4. The shipped index holds 716 chunks of the original and 587 of the consolidated in one file at `python/data/index/annex.db`, which is derived and gitignored, and rebuilds in 27 seconds. `pgvector` costs a service and a connection string for operational realism nobody exercises inside this window. Measured in PR #7 on 2026-09-07.

  The original's count moved on `feature-corpus-and-answer-text`. `annex.corpus.parse_oj` no longer folds the Official Journal's signature block and footnotes into `art_113`, so that provision falls from 14 847 to 535 characters and stops needing a split. The index holds 714 chunks of the original and 587 of the consolidated, unchanged there. Measured at this branch on 2026-09-13.

  Both counts moved again on `feature-cut-provisions-at-headings`. A chapter heading is now its own addressable provision rather than text folded into the article before it, 13 in each version, and the existing chunk rule already covers a provision carrying no paragraph of its own without a rule written for it. The index holds 727 chunks of the original and 600 of the consolidated. `canon/context/retrieval.md` carries the same figures against the chunk rule itself. Measured at this branch on 2026-09-14.

- **Graph representation.** Settled by measurement rather than by preference. `networkx` declares `requires_python: !=3.14.1` against this project's pinned 3.14, so it cannot be imported at all, and the graph is a plain in-process adjacency map over parsed references. At hundreds of articles and hundreds of edges that carries the two operations this project needs, a bounded walk and a reverse lookup. A graph database stays unjustified at this scale.
- **Chunking strategy. Partly settled, and the open half is still open.** The embedder decided the rule before retrieval taste could: both models run here hold 2048 tokens and truncate over-length input silently, and article-level chunks do not fit, so a paragraph is a chunk, a provision carrying no paragraph is a chunk, and a chunk still over budget splits on its own `(n)` points. That covers 712 provisions in the original and 585 in the consolidated. The budget was set from real token counts read off the embedder rather than from the document-wide 5.03 characters a token, since the real ratio runs 3.13 to 6.43. `canon/context/retrieval.md` carries the figures. The cheaper experiment this entry named has now been run: `nomic-embed-text` was compared against four alternatives and `snowflake-arctic-embed2` won and ships, taking search alone from 0.41 and 0.46 to 0.61 and 0.71. `store.py` moved to 1024 with it and `write` now takes the width as a keyword, so a candidate at another width needs no source edit. What remains open is the question this entry originally asked, whether these units retrieve better than fixed-size windows, and one new fact bears on it: `snowflake-arctic-embed2` declares 8192 of context while Ollama serves 2048, so article-level chunks would need a Modelfile for the embedder and would cut the index from 714 and 587 chunks to 113 and 119. That is its own comparison and the harness can now decide it. Measured in PR #7 on 2026-09-07, the split count corrected at this branch on 2026-09-13.
- **Frontend scope. Partly settled, and the graph-visualization half is open again.** Three layouts were rendered side by side, varying only where citations sit, and inline under each claim won. Proximity never breaks, and the requirements make visible source text what separates this from a chatbot. The cost is measured: inline spreads three claims over 471px against a right rail's 216px, and apparatus weight brings that to 356px. The reference graph was recorded as not visualized, closing the question this entry once left open. Both themes ship. `canon/wireframes/answer.md` carries the layout, `canon/DESIGN.md` the tokens. Measured in PR #2 on 2026-09-06.

  That closure did not hold. Whether the graph is drawn at all is reopened here as of 2026-09-12: a follow-up branch runs its own rendering comparison over candidate arms and records the picked arm in this entry against what it beat. This row carries no rendering of its own to hand it.

  A second question this entry left implicit is now measured rather than assumed: whether a static export can carry the whole corpus for a reading panel, rather than only the answer fixtures. Both versions, at article, annex, recital and paragraph granularity, serialize to 958 155 and 789 949 bytes of JSON, 156 783 and 105 608 gzipped, 262 391 bytes combined. The build already ships 744 KB of answer fixtures across 24 files, so this is the same order of magnitude rather than a new class of problem. The narrower export, holding article, annex and recital text alone and landing a citation at the top of the article rather than on its exact paragraph, costs 233 KB gzipped combined against 330 KB for the wider one. The wider export shipped: it is the only option that lands a citation on its own provision rather than on the article carrying it, and the gzip difference is smaller than one already-shipped answer fixture. Measured at this branch on 2026-09-12.

  Those bytes moved again on `feature-cut-provisions-at-headings`, and not in the direction the heading fix on its own would suggest. Re-exported after that branch's parse change, `original.json` and `consolidated.json` run to 997 231 and 833 096 bytes, 158 691 and 108 607 gzipped. The heading fix trims text, but three merges landed on `main` between the figure above and this one, and this entry has never carried a per-commit reconciliation of what moved the export between two measurements. The numbers here are what the export is now, not a claim about what the heading fix alone cost it. Measured at this branch on 2026-09-14.

  The graph-visualization reopening closed on 2026-09-12, on the follow-up branch it named. Four arms went through `canon:draft-and-pick`'s render-and-pick loop against a realistic fixture shape, twelve searched and forty traversed provisions over three hops, in both themes: the three id lists as they stand today, a hand-drawn layered SVG with hop as the column, a hand-drawn arrangement with no layered axis, and a tree built with `d3-hierarchy`. The operator picked the layered SVG. Hop is already computed by the walk, so the column assignment is arithmetic rather than a layout algorithm, it needs no dependency, and it inherits both themes through `currentColor` the way every other element on this surface does. The non-layered arm was rejected on the render itself: with no natural axis to place a node on, the arm falls back to a hash of each id, and legibility visibly degrades as the node count grows. The `d3-hierarchy` arm cost a dependency and a synthetic root to unify the walk's forest of up to twelve seeds into the one tree the library expects, for a result no more legible than the hand-drawn column at this node count. `RetrievalTrace.edges` is what a drawing needed and did not have, one edge to each traversed provision naming what it was reached from, and `web/src/components/traversal-graph.tsx` is what draws it. `canon/wireframes/answer.md` § Not on this surface, which stated the graph was not visualized, is retired.

  The walk settles which column a provision sits in and says nothing about which row, and the first render against real fixture data showed that silence: every column left at its own height crushed a short one into a narrow band, and reading the render against a fixture with a wide fan-out read as a tangled knot rather than an expansion. `traversal-graph.tsx` now sorts each column by the row its own edge's source already landed on, which is the crossing-reduction pass `dagre` and `elkjs` were rejected for supplying, done as a direct sort over a tree with one parent per node rather than a general algorithm searching for one. The rejection above still holds. What it left unstated is that hop alone was never the whole layout, and row order is the part a library would have carried for free. Measured at `182598c` on 2026-09-12.

  The layered drawing did not survive the first-use pass, and this entry keeps it rather than deleting it, since what it beat is part of the record. The operator ran the wait and the finished walk through the same draft-and-pick loop on 2026-09-13, picking arm 3b for the wait (O2) and arm 3 for the finished walk (L4 and C3): a row per search result beat today's drawing cropped from the replay build, the wait's own chips relabelled with pipeline headings, and plain words carrying no hover or click. L4 found the drawing unreadable at 400 pixels, and the draft that shrank it to share the pane with the reading card put its type near 6 pixels. `web/src/components/walk-chips.tsx` lays the walk out a row per search result instead: the paragraph search found, the article the walk lifted it to, and what that article cites as wrapped chips, with a group a hop further headed by the chip that reached it. A row per result also retires the row-order sort above, since every edge has one source and a provision always sits in its source's row. `traversal-graph.tsx` is removed. The rejections of the non-layered arm and of `d3-hierarchy` still stand, and the chips need neither a layout algorithm nor a dependency. Measured at `b93e8be` on 2026-09-14.

  The inline decision above was answered at three short claims, and the surface as recorded does not look like that. Across the 18 recorded answers the claims run to a median of 1 384 characters against 15 849 of quoted law, and 65 of their 112 citations pass 1 000 characters. `canon/context/design-references.md` records twelve products read and six documentation sites measured, and three rounds of four candidates each were rendered against the first recorded answer, citing Article 3 whole at 17 615 characters, and a refusal consulting 20 provisions at 67 320, at 1280, 1536 and 400 pixels wide.

  The first round varied how a claim is paired with its statute, and the operator picked a six-line excerpt under each claim in a column near 640 pixels, beside a docked pane holding the whole Act. Printing every provision whole put the second claim 2 836 pixels down at 1280, where the excerpt puts it at 540.

  A claim row with a sticky aside left tall blanks beside short claims and measured the refusal at 11 379 pixels. A peek card with no excerpt was the shortest page and left no statute on screen until a click. Proximity survives from the inline decision and full quotation does not.

  The second round varied what the pane holds before anything is asked, and the operator picked the terms strip and a reserved region for the three-arm comparison. It beat no pane until an answer exists, the Act open at Article 3, and a recorded answer previewed, which answered a question nobody asked and depended on one fixture staying answered.

  The third round varied where the trace sits, and the cost line stays at the foot of the answer column while its counts open the walk as the pane's second view. Inline at the foot, the walk squeezed 570 pixels of drawing into a 608-pixel column. Above the answer, the cost line put machine output ahead of the claims, and in the pane header it left the answer column at one width and returned to it at another.

  Three recorded lines were overturned in part. The wireframe's rule that the reading panel is the only overlay and opens only from a citation now holds below 1024 alone, and at 1024 and wider the Act is a region of the one screen.

  The line that nothing on the surface is a route a visitor could bookmark gave way to the deployed build carrying the question, the version and the open provision in its address, which keeps one page and adds no route. No navigation rail, no account and no history all stand. Measured at `f486520` on 2026-09-12.

  The fixed frame did not survive being used. The operator's first-use pass on 2026-09-13 picked four changes by looking, each against rendered arms. The top bar pins and slims once the described system scrolls under it, which beat a fixed bar, a full pinned bar and a slim bar carrying the question. Measured on the old frame, the version toggle left the screen after 63 pixels of scroll and the pane started 222 pixels down with its last 222 pixels below the fold.

  The answer column's fixed 640 pixels became a default a reader can drag between 480 and 760, remembered and applied before paint. It beat the fixed split, a handle with three preset widths, and a single toggle. The pane at 1280 had been about 560 pixels wide, which is what squeezed both the walk and the pipeline figure.

  The Act pane gained a section bar naming the section in view and its place of 133, stepping through the whole Act or through the answer's citations, with a jump field and the arrow keys. It beat the cited links alone, step buttons in the cited row, and the same bar without a jump field or the switch. The operator's one condition, that a provision lands flush under the bar, retires the docked half of the heading-in-view landing rule.

  The overlay below 1024 kept that rule for a reason `feature-failing-e2e-cases` found stale: `act-reader.spec.ts:165` asserted the rule as unconditional, and Article 50(5) does not fit its own heading in the overlay's 797-pixel body, so the paragraph landed flush with no heading in view, which the test read as a defect. The fit-conditional landing is unchanged, since the operator's own pick kept it. What changed is the reason the overlay needed it at all: a one-line sticky label now names the open article at the top of the overlay body, always in view regardless of scroll, a minimal analog to the docked pane's section bar rather than the bar itself, since it carries no jump field and no step buttons. Measured in PR #36 on 2026-09-14.

  At 400 pixels the bar holds the brand, the recording band shortens to one line with its details a tap away, and the version and traversal choices sit under the description. The heading starts at 119 pixels and the input at 391, where the old page put them near 330 and 600. The draft behind that pick had cut the supporting paragraph short, and the operator kept the full paragraph and its order once the built page measured the input lower. Measured at this branch on 2026-09-13.

  The favicon and the pipeline figure carried O1 and O4's own defects into their builds, and both were checked against the operator's picked render itself rather than only against the pick's prose summary, which is what caught a real gap: the picked renders (layout arm 3, style arm 0) draw Retrieve and Traverse with solid accent-filled dots against the other three stages' hollow ink-bordered ones, and the bracket and its caption in the accent color, none of which the pick's own sentence stated. A first build read the sentence alone and shipped uniform ink dots and a muted caption, where the corrected build matches the render. `web/public/favicon.svg` carries a `prefers-color-scheme: dark` block swapping its ink lines and center node to `#EDEBE6` and its accent ring to `#8FB2E0`, matching the picked dark-mode favicon render (arm 1), following the browser or system theme rather than the page's own toggle since the file loads before either script runs, and `web/src/components/brand-mark.test.tsx` now asserts the header mark's geometry against the favicon's own on every run so the two cannot drift apart. `web/src/components/pipeline-diagram.tsx` redraws as a vertical rail, one stage a row at full size, with every label sized in plain HTML pixels rather than an SVG viewBox unit, holding an 11px floor measured at 1280 and 400 against the 7.1px the first-use pass measured, checked off the built page rather than judged by eye. Measured at this branch on 2026-09-13.

  Pick 15, the social card, ships its generator and not its output. `web/scripts/generate-social-card.ts` renders the picked arm from a recorded fixture rather than typed text, proven to run locally against `web/src/fixtures/q04-cv-screening.consolidated.json` and checked by eye against `card-arm-1-1200x630.png`, but `feature-corpus-and-answer-text` re-captures every recorded answer and has not merged, so committing `og-image.png` or `og-image-alt.png` now would ship a card quoting a claim that branch is about to replace. The page still serves the mark-and-name card shipped in #13 until that branch merges and the generator runs for real. Measured at this branch on 2026-09-13.

  The hold closed on `feat/social-cards`, which this entry stayed out of so that branch could own it. `feature-corpus-and-answer-text` merged as `#28`, so `bun run generate:social-card` ran for real against the re-captured fixture: the claim still names Annex III point 4(a), still cites `anx_III`, and the citation still reads `changed: false`, so the generated card matches the arm pick 15 chose rather than a placeholder. `web/public/og-image.png` and `web/public/og-image-alt.png` are both committed there, at 1200 by 630 and roughly 82 and 88 kilobytes, and `web/e2e/metadata.spec.ts` asserts both resolve over HTTP with a non-zero body. Arm 3's panel ran 40px past the canvas edge on the first render, the width of its right cell carried straight over from the operator's picked render with no narrowing applied, and narrowing that panel from 760px to 710px keeps it inside the 1200px canvas. `web/src/app/layout.tsx` still points every consumer at `og-image.png` alone. Nothing automatic keeps either card in step with the next re-capture, and a fixture change that breaks one of the generator's three preconditions fails its own throw rather than the build. Measured on `feat/social-cards` at `cf3510f`, ahead of that branch's own merge.

  Answer column picks from the same pass ship on `feat/answer-column`, and one of them is decided by a rule rather than by a render: where an excerpt opens. Each cited provision is cut at its own paragraphs, definitions and points, and an excerpt opens on the passage sharing the most words of four letters or more with its claim once that passage shares 8, with the heading naming it the way the Act cites it. Looking at three cases fitted the threshold of 8. Word length was measured instead: rerun over the 47 recorded citations past 2 500 characters, counting words of four letters or more matches the picked draft's decision on 42 and its landing on 39, where counting every word matches 26 and 21. A per-citation hand check waits on the re-capture `feature-corpus-and-answer-text` owns, since that decides which claims exist, and `web/src/lib/closest-point.ts` carries the rule. Measured at this branch on 2026-09-13.

  The amendment half held on `#26` for the reason recorded there, that the corpus export still carried consolidation markers a word diff would have read as inserted words, ships here. `#28` stripped those markers from the parse, closing the hold, and `web/src/lib/amendment-diff.ts` reads a word diff off the two exported corpora rather than off the markers: 42 of the 117 recorded citations read `changed: true` over 17 of the 24 fixtures, resolving to 16 distinct provision ids, and every one of the 38 that exist in both texts differs on at least one word, so the diff has something to draw on all of them. The remaining 4 name `art_111.4` and `rct_132`, present in only one version, which read `added by the amendment` or `removed by the amendment` in place of `moved`, with the excerpt whole and no version control, rather than drawing an empty diff. `citation-block.tsx` highlights the words the shown version does not share with the other on `warning-surface`, the same ground the amendment note stands on, underlined in `cite-rule-moved`, and a text control reading the other version's own name swaps the excerpt for that version's whole text and its own highlight, reaching a removal without ever striking a word out of the Act's own quoted text. The tint alone was not the highlight it read as: `warning-surface` against paper measures 1.057 to 1 in light and 1.167 to 1 in dark, caught in review against the `4-answered` capture on `#31` where no word of the quoted provision read as marked. `cite-rule-moved`, the border the moved-citation rule and chip already carry, measures 3.09 to 1 against `warning-surface` in light and 3.63 to 1 in dark, both past the 3 to 1 floor a non-text mark is held to, and reuses a token this citation's own moved state already draws with rather than adding one. The diff is memoized once per provision id at the module rather than once per citation, since the 42 changed citations resolve to 16 ids. `act-reader.tsx` carries the same highlight onto the Act pane, bounded to the one section a citation opened rather than diffed across the whole Act: an article's own provision record duplicates its children's text at offsets a child's diff cannot read, so each paragraph in the open section is diffed under its own id instead of sliced out of its parent's. `art_3` is the longest single provision either text carries, at 17 600 characters against the 7 350 a fixture like `anx_III` runs to, and it is a recorded citation rather than a worst case nobody reaches. A first diff of it, before anything cached the result, measured 66 milliseconds on this machine, which answers the question the plan opened rather than closing it by assertion: the memoization already in place is what keeps that cost to the one provision's first open. Measured at this branch on 2026-09-13.

  A second first-use pass, run with the operator on 2026-09-14, closed four more findings against rendered arms, split across two branches by which files each pick touches. The half landing here carries no file the other rewrites: the divider between the answer column and the Act gains a 10 by 40 pixel grip, bordered in `cite-rule` on `surface` and holding three dots, reading 3.13 to 1 against paper in light and 3.37 to 1 in dark at rest, past the 3 to 1 floor a non-text control is held to. Its round 2 correction stands rather than the round 1 draft: hover darkens the line and the grip to neutral ink, never accent, and accent is reserved for keyboard focus alone, drawn as a border and a focus ring together. Building it exposed a defect the pick's own measurements never carried, since a three-dot row laid out across the grip's 10 pixel width overflows a container that narrow, and the dots stack in a column instead, caught only by reading the rendered evidence capture rather than the class list alone. The replay band drops its short commit hash and reads instead "You are looking at a recording. Nothing on this page calls a model. Every answer was captured from the live system on `<date>`.", with the short form below 1024 keeping only the first sentence, and the terms strip drops its matching "Commit hash" entry, since a glossary term for a thing no longer shown explains itself rather than the page. Every `uppercase` label this branch can reach becomes sentence case in the page's sans font rather than tracked mono caps: 16 sites across 11 components as first built, plus one more the rebase onto `#36` exposed, the overlay's own article label in `act-reader.tsx`, carrying the identical treatment and swept in the same pass. `recorded-picks.tsx` keeps its one site, reserved for the row rebuilding that file whole. `web/evidence/` was recaptured against the rebased tree rather than the pre-rebase one, since `#36` self-hosts the font every glyph on the surface draws in and moves the rendered pixels of every state. Measured at this branch on 2026-09-14.

  The rest of that pass's picks that work on today's one route land on `feat/composer-and-answer-card`, and two of them reverse lines this entry records rather than extend them. The top bar no longer slims. The first-use pass picked a bar that dropped its tagline and links once the described system scrolled under it, over a fixed bar, a full pinned bar and a slim bar carrying the question. The operator reversed that to the full pinned bar on 2026-09-14 without a draft, so `use-scrolled-past.ts` is removed with its one consumer.

  The Act pane's version toggle is gone as well. It swapped only the pane's text while the bar's identical toggle re-asked the question, so nothing on the surface said which one changed the answer. N12 arm 3 moved the answer's toggle into the described-system card and reduced the pane to a line naming the text it shows, with a link to the other. It beat two identical toggles, the bar's toggle alone with no way left to read the other text in the pane, and both kept with an `Answer against` label on the bar's.

  Four picks build around those reversals. The composer holds the description, both choices and `Find the articles` at every width, N4 arm 2, with the traversal hint kept as visible text where the draft had shrunk it to a chip. Each recorded question is a bordered card whose outcome line `web/src/lib/recorded-outcome.ts` computes from the manifest's `refused` flags, N3 arm 1, drawn without the draft's dot, which read as one dark mark in both themes. The described system becomes a card at the head of the answer column, N11 arm 1, and holds the only version toggle once something is asked. How an answer is built and the comparison share one section, N10 arm 3, which goes two columns only in a container 1 040 pixels wide. The single column never reaches that, and by the grid's own arithmetic the docked pane reaches it only on a window about 1 760 pixels wide or more, so 1280 and 1536 draw it stacked until a landing page gives it the room. The flow label in `recorded-picks.tsx`, the one site the paragraph above left in the retired form, converts to sentence case with the cards. Every losing arm behind these four picks was measured in the same second-use pass. Built at this branch on 2026-09-14.

  The one page did not survive that pass either, and two lines above give way to it rather than being deleted. N1 arm 2 split the surface: `/` is a landing page with the heading, the supporting text and the composer centered under the bar, the recorded questions full width, and the terms and the three-arm comparison as sections below them, and `/ask` is the two-column tool a pick or a submit navigates to, with the back action and the mark both returning to `/`. It beat arm 0, the one route swapping in place, arm 1, which kept the split home and added `/ask` beside it, and arm 3, an introduction-only home that put the input on `/ask` and took two clicks and a third screen state to reach an answer. The second-round pick of the terms and a reserved comparison region in the pane before anything is asked is retired with that pane, since nothing sits beside a composer on its own page. The line that the address keeps one page and adds no route is retired too: the address still carries the recorded question, the version and the open provision and nothing more, now on `/ask`, and a link shared as `/?q=` forwards to the same answer there. A typed description crosses the navigation in memory the root layout holds rather than in storage or the address, so a reload of `/ask` on the live build has nothing to ask and returns to `/`. The end-to-end run serves the export through `web/e2e/serve-export.ts`, since `python3 -m http.server` answers `/ask` with a 404 where Cloudflare Pages resolves it to `ask.html`. On the built export at 1280 the input sits at 371 pixels and the first card at 696, against the draft's 372 and 657, the difference being the full supporting paragraph and a composer holding its traversal hint as visible text. The comparison's two columns, which no screen on the one route was wide enough to draw, first draw here in a 1 080-pixel container at 1280 and 1536, with the bracket 12 pixels from the stage text and no figure type under 12 pixels, and stack at 400. Measured at this branch on 2026-09-14.

  A third-use pass, run with the operator on 2026-09-14, closed four more findings, three of them in `act-reader.tsx` and the fourth filed mid-build by the session that ran the pass. The amendment mark set a background and a border and no text color, so a changed word kept the browser's own black, measured at 1.36 to 1 on its tint in dark against `act`'s own 6.88 to 1 there. It now inherits the color of the text it sits in, in both `act-reader.tsx` and `citation-block.tsx`, reaching that same 6.88 to 1 rather than a fourth color. A run of changed words separated only by whitespace also joins into one mark with one unbroken underline, punctuation still breaking a run: Article 50(7) drew 38 marks and now draws 8, joined once by a pure helper applied after `sliceDiffSpans` and `trimDiffSpans` rather than by marking the gap itself changed, which would have claimed unchanged whitespace differed between the two texts. The composer textarea drew the browser's own square outline 17 pixels inside the card's 14-pixel radius. It now draws none, and the card's own border darkens from `rule` to `muted` while anything inside it has focus, textarea, toggle, switch or submit alike, measured at 5.38 to 1 and 6.18 to 1 against the 1.33 to 1 and 1.30 to 1 it rests at, with `border-error` still winning on a failed submit. The docked landing sat the tint half a pixel under the section bar's own border, with the previous provision's tail showing above it. A 12px paper band now pins under the bar and a landing stops 12px short, closing both halves together, since a stop with no band still shows that tail and a band with no stop leaves the tint flush. A landed paragraph also takes the rounded, padded tint a landed section already carried rather than a square edge-to-edge one. The pane's view switch, `ViewButton`, filled its selected label with `bg-ink` where `top-bar.tsx`, `theme-toggle.tsx` and the section bar's own `Whole Act` and `Cited` switch all fill with `accent`, reading as a near-white block in dark. It now draws as tabs, the selected label in `ink` over a 2px `accent` underline and the unselected one in `muted` over a transparent underline of the same width, with no fill and no wrapper box, keeping the browser's native outline as its focus mark since the underline already carries selection. The third-use operator pass carries the arms and the measurements behind all four. Measured at this branch on 2026-09-14.

  That same third-use pass named a fourth gap the second-batch queue opened as its own task, `v5.9`: the rail leaves most of its own row empty wherever the stages and the comparison table stack, 42 percent of 810px and 54 percent of 1024px. `pipeline-diagram.tsx` now carries a second drawing, a horizontal row, both reading the same `STAGES`, `BRACKET_START`, `BRACKET_END` and `CAPTION` constants the rail already used, so a wording change reaches both. The two switch on the component's own rendered width through a `@container` query, the pattern `walk-chips.tsx` already uses, rather than on `comparison-argument.tsx`'s stacked state, since a docked pane can hand the figure a narrower slot than the landing page's own stack breakpoint implies. The row shows from 657px of the component's own width, measured on the built page at a 1024px window by narrowing until the shortest label started wrapping. Building the row's bracket first put it as a sibling of the label grid rather than a grid item inside it, so its `gridColumn` inline style did nothing against a plain block parent and the curve drew across the whole row instead of under Retrieve and Traverse alone, a defect `getComputedStyle` still echoed back correctly and a DOM-presence test never caught. Folding the bracket into the same grid as a third row fixed it, checked against the picked render (arm 1), which the drawing now matches: the bracket scoped to Retrieve and Traverse rather than the full-width curve the first build drew. The empty space measured at the third-use pass is 0px at both 810 and 1024 once the row shows, since the grid's `minmax(0, auto)` columns stretch to the component's full rendered width. `web/e2e/home.spec.ts` asserts the row over the rail and the 11px floor at both widths. Evidence capture waits on `feat/cut-provisions-at-headings` merging first, so `web/evidence/` does not move on this branch. Measured at this branch on 2026-09-14.

  A second batch out of that same third-use pass, T6, found the landing page carrying too much past the recorded question cards: the terms, "How an answer is built", the three-arm comparison and the run notes, measured at 850px of 2 060px total at 1280 and 1 671px of 3 783px at 400, 41 and 44 percent of the page. Pick 5 picked arm 3: those sections move to their own `/evaluation` route, under an `Evaluation` heading, a line naming what it holds, and a link on to `docs/evaluation.md`, and `Evaluation` in the top bar opens that route in-site rather than leaving for GitHub. It beat arm 0, the page as shipped, arm 1, reaching the route from the bar alone with no way in from the page a visitor lands on, and arm 2, folding the sections behind one closed disclosure under the cards with no new route at all. The candidate landing page measured 1 342px at 1280 against the shipped 2 060px, and the new route itself 1 045px. It revises N1 arm 2 and N10 arm 3 from the second-use pass in part: the terms and the comparison stop being sections of `/`, and the side-by-side stages and table this entry already records move with them to `/evaluation` unchanged. `terms-pane.tsx` keeps rendering the same two components beside a timeout or an unexpected failure on `/ask`, untouched by the move. Built on `feature-evaluation-route` on 2026-09-14.

- **Prompt injection surface is narrower than it first appears.** The corpus is trusted public law. The untrusted input is the user's own system description. Worth defending, not worth overstating.
- **The service meets the server-security bar except on transport, and the gap is stated rather than silent.** `.claude/rules/canon/lib/360-security-server.md` asks for a bounded body, a bounded request, and a CORS origin list that is never reflected and never a wildcard. All three ship. TLS does not: the service binds to `127.0.0.1` for a local demo, where a certificate defends a loopback hop against nobody and costs a trust store on every machine. Authorization and rate limiting are not gaps but absences, since accounts are deferred in `canon/REQUIREMENTS.md` and one card holds one model, so concurrent asks queue inside Ollama rather than multiplying cost. What a deployment off loopback has to add is TLS, and nothing in the code will notice on its own. `canon/context/service.md` carries the endpoint's own record.
- **Hosting. Settled, by conceding the half that was never answerable.** The static half deploys to a Cloudflare Pages Direct Upload project at `annex.erclx.dev`, pushed from `.github/workflows/deploy.yml` with `cloudflare/wrangler-action`, gated on the web checks and uploading the export those checks tested. Cloudflare runs no build, so no root directory is configured outside this tree. Two acts remain the operator's and a worker cannot take either: creating the Pages project and attaching the custom domain, so the workflow lands ahead of the first production upload rather than with it.

  The half this entry could not answer was where the model runs, and the answer is that it does not. Nothing hosted answers these questions for free and the local one holds 30 GB of a card, so the deployed build asks nothing: `uv run python -m annex capture` puts the twelve evaluation questions through the real pipeline on both versions, the answers are committed under `web/src/fixtures/`, and the page replays them behind a band saying so. That is a deployment of the surface and the record rather than of the system, and stating which is the whole reason the band is on the page instead of in a footnote.

  What it costs is stated rather than hidden. A fixture goes stale against any change to prompts, chunking, retrieval or the corpus, and only the schema half of that is visible: the strict parse fails on a field that moved and nothing fails on an answer that no longer matches. The manifest carries the commit and the date, a re-capture is minutes of GPU, and `canon/context/service.md` carries the seam. The traversal switch is inactive on that build, since the capture ran with the walk on and one recording cannot answer both positions. Settled at this branch on 2026-09-07.
