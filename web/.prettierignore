# Captures, and the design round they are measured against. The round is an
# imported artifact carrying its own formatting, so it is kept as it was
# rendered rather than reformatted into this project's style. The root
# `.prettierignore` and `cspell.json` exclude it for the same reason, and this
# file is what applies that to the web half's own prettier run.
evidence/
